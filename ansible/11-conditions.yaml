- name: check conditions
  hosts: local
  connection: local
  vars:
    NUMBER_OF_SESSIONS: 7
    IS_LIVE: false
  tasks:
  - name: is ansible more than 5 sessions and is it live
    ansible.builtin.debug:
     msg: "yes it is true"
    #when: NUMBER_OF_SESSIONS > 5
    when: IS_LIVE 