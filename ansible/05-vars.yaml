- name: file level variables
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: print var
    vars:
     COURSE: Ansible
    ansible.builtin.debug:
      msg: "course is {{COURSE}}, trainer is {{TRAINER}}"