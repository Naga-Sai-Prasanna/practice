- name: play level variables
  hosts: local
  connection: local
  vars:
    COURSE: "devops"
    DURATION: "140HRS"
    TRAINER: "U"
  tasks:
  - name: print var
    vars:
     COURSE: Ansible
    ansible.builtin.debug:
      msg: "course is {{COURSE}}, trainer is {{TRAINER}}"